// Phase 1 scaffold: metrics controller interface
export const MetricsController = {
  // totalSales(req,res) {}
  // totalOrders(req,res) {}
  // aov(req,res) {}
  // cvr(req,res) {}
  // funnelStats(req,res) {}
  // deltas: salesDelta, sessionsDelta, atcDelta, aovDelta, cvrDelta
};
